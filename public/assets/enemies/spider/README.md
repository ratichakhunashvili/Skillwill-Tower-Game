# spider enemy art (optional)

Drop a single PNG named `spider.png` here (static image, transparent background, facing right) to replace the generated placeholder spider on every floor. Not required — the game auto-generates one if this is missing.
