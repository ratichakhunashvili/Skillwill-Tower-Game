# themed mob art (optional)

Drop a single PNG named `mob.png` here (static image, transparent background, facing right) to replace the generated placeholder "weird mob" used on every floor. Not required — the game auto-generates one, tinted per floor's theme color, if this is missing.
